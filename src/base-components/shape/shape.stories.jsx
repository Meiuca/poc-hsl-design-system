import React from 'react';
import DSShape from './index.js';

export default {
  title: 'Base Components/Shape',
  component: DSShape
};

const Template = () => <DSShape></DSShape>;

export const Default = Template.bind({});